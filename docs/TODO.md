# List of TODOs

## Candidates

- **Level discoverability scaffolding across the review-agent cluster.** Boundary audit (2026-06-06) of the spec/plan/arch reviewers found the cluster well-bounded (one per artifact × pipeline stage — not bloat), but scaffolding is uneven: `spec-checker` has neither `## Not to be confused with` nor `## When NOT to use`, and `spec-reviewer`/`contract-reviewer` carry only the former. Bring all up to the `plan-reviewer` standard. Highest payoff is `spec-checker`, the most name-confusable agent (vs `spec-reviewer`, opposite job: post-impl code/test verification vs pre-impl doc quality) yet the weakest-scaffolded — and it doesn't point back at `spec-reviewer`. Optional escalation (breaking, major bump): rename `spec-checker` → stage-clear name (`rule-verifier` / `impl-verifier`), since it reads code+tests, not "the spec."
- **EARS-style acceptance criteria in `spec-writer` (borrow from Kiro).** Adopt the "WHEN [event/condition] THE SYSTEM SHALL [behavior]" requirement syntax for spec acceptance criteria. Makes requirements formally testable and feeds the test-writers directly — low effort, serves the SDD bet. Pairs with `spec-reviewer`'s rule-quality check.
- **Extract the methodology into one `METHODOLOGY.md` (stack-agnostic).** Today the SDD method (spec → contract → plan → test-first → implement → review → verify; the a/b/c review discipline; TRIGRAM rules; branch/PR/release gates) is distributed across `CLAUDE.md`, agent bodies, and `README.md` — never stated in one place, while the Tauri/Svelte specifics already live isolated in `kit/docs/*.md`. A token-density scan (2026-06-06) confirms the kit already cleaves ~80% along this seam: the doc/process agents and skills are near stack-agnostic (0–4 stack tokens) while the code-touching reviewers/test-writers carry all the stack flesh (12–83). Writing the abstract file makes the method a portable, nameable asset (the kit's SDD-flavored answer to Superpowers), shared across the React/Svelte branches while only the stack docs fork. Doc-extraction only — NOT a full method-core/stack-pack architectural split (that revives the dropped v4.0 profiles; the branch-per-framework + `kit.config.json` model already handles variation).

## Experimental

- **Trial rubric-driven doc reviewers on `sonnet`.** The doc reviewers run `opus` (`spec-reviewer`, `contract-reviewer`, `plan-reviewer`, `spec-checker`) while `adr-reviewer`/`reviewer-arch` run `sonnet`. `plan-reviewer` is heavily rubric/coverage-driven (mechanical checks A–I) — measure the quality delta on `sonnet` for a downstream cost saving. Confirm the opus/sonnet split is a deliberate decision, not drift.
