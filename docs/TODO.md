# List of TODOs

## v4.4 candidates

- **`frontend-rules.md`: a11y labels must be translateable.** Add a rule requiring `aria-label`, `aria-labelledby`, `aria-describedby`, `title`, and `placeholder` strings to flow through the i18n layer (e.g. `t("…")`), not be hard-coded. Hard-coded a11y strings are silent i18n holes — they pass visual review and TypeScript checks, but ship untranslated to non-default-locale users. Reviewer rule should flag any literal string passed to those props. Cross-link to `reviewer-frontend` so the audit lane catches it on PRs.

- **`frontend-rules.md`: components must define stable `id` attributes.** Add a rule requiring components (or at least their primary interactive elements — buttons, inputs, dialogs, list items) to render a stable `id` prop. Two motivations: (a) RTL/screen-reader navigation benefits from stable anchors and `aria-labelledby` references, (b) E2E tests (WebdriverIO / Playwright) become brittle when keyed off class names or text content — stable ids make selectors deterministic across refactors. Open design call: id-naming convention (e.g. `{feature}-{component}-{role}` like `account-list-item-edit`?), and whether to mandate at the leaf-component level or only at page/dialog roots. Likely pairs with a `reviewer-frontend` lane addition.

- **Rust DDD project-structure layout + Shared Kernel guidance (issues #18 + #19).** Bundle — #19's text references `shared/domain/` so the layout decision must land first.
  - **Design call required before implementation.** Issue #18 asks 5 explicit questions (top-level naming `shared/` vs `core/` vs `platform/`; `infrastructure/` vs `infra/`; mandate flat-first inside infra or leave to project; shared-kernel folder name; edge cases). Pick answers before writing the kit PR.
  - **#18 — new "Project structure (Rust)" section in `backend-rules.md`** with the gold layout (`shared/{application,domain,infrastructure}/` + `context/{bc}/{api.rs, application/, domain/, infrastructure/}` + `use_cases/{flow}/`) and 6 conventions: three layer-named folders symmetric across BCs and `shared/`; `api.rs` single file at boundary; `infrastructure/` not `repository/`; flat-first inside `infrastructure/`; `shared/` not `core/`; keep layer folders even when small.
  - **#19 — new "Shared Kernel" sub-section in `ddd-reference.md`** explaining what it is, what lives there, what doesn't, where in the layout (`shared/domain/` per #18), and the discipline (every change requires agreement from every consuming BC; keep small; default to use cases / domain events instead). Also softens the existing "BCs ... never through shared domain objects" line into a documented exception.
  - **Cross-link** `ddd-reference.md` § Application Layer → `backend-rules.md` § Project Structure.

## v4.5 candidates

- **`mirror-local.sh`: also mirror `*.py` from `kit/scripts/`.** Currently only `*.sh` is mirrored, so `kit/scripts/whats-next.py` (and `release.py`) are not available at `scripts/whats-next.py` during kit development — `/whats-next` and similar skills fail in this repo even though they work downstream. Fix by extending the loop in `scripts/mirror-local.sh:43-51` to also copy `*.py`, but **explicitly exclude `check.py` and `release.py`** because kit-internal versions of those already exist in `scripts/` and must not be clobbered. Safest approach: explicit allowlist (`whats-next.py` for now), not a glob.

- **Fix `just merge` on squash/rebase merges (issue #14).** `just merge` fast-forwards local `main` from the local branch SHA, which orphans `main` from `origin/main` whenever GitHub uses squash-merge or rebase-merge (different SHA, same diff). Next branch cut from local `main` then surfaces phantom merge conflicts on the new PR. Fix lives in `common.just`'s `merge:` recipe — detect drift via `git cherry origin/main <branch>` (patch-id comparison): if every branch commit's patch is already on `origin/main`, `reset --hard origin/main` and `branch -D`; if branch has true unmerged commits, refuse and tell user to rebase. Full reproduction, three-case decision table, and proposed recipe in the issue. Reproduced on real incident (`phileggel/VaultCompass` PR #11). High priority — silent corruption.

- **Pre-commit hook should skip gracefully on empty-stack repos (issue #15).** First-ever bootstrap commit (after `sync-config.sh` on a fresh `git init`) is rejected by the synced pre-commit hook because `check.py --fast` fails on absent `package.json` / `src-tauri/`. Worst possible UX moment — user's first impression nudges them toward `--no-verify`. Fix: extend `kit/scripts/check.py` to treat missing stack inputs as `SKIPPED` rather than `FAIL` (same spirit as existing skip rows for shellcheck/Prettier when not installed). Issue lists 4 alternative fixes; option 1 (graceful skip) generalises to partial-stack states during incremental scaffolding. Decide between option 1 (graceful skip) and option 2 (path-based skip for kit-only commits) before implementing. High priority — first-impression UX.

- **Evaluate kit behaviour on no-DB Tauri projects.** A new Tauri app may legitimately not need SQLite (or any database). The kit currently assumes SQLx is part of the stack in many places; identify the surface area before deciding on fixes. Three axes to audit:
  1. **Scripts** — no script failure observed in the field so far on a no-DB Tauri project (`kit/scripts/check.py:96` cleanly skips when `.sqlx/` is absent, which is likely why). Still worth a sweep across all `kit/scripts/*` and any DB-touching agent helper scripts to confirm they all skip-not-fail when `.sqlx/`, `migrations/`, or DB env vars are absent — and to catch any partial-stack edge case (e.g. `migrations/` exists but `.sqlx/` doesn't, or the reverse).
  2. **Agents/skills that give incorrect or misleading advice** — sweep `kit/agents/*.md` and `kit/skills/*/SKILL.md` for hard SQLx assumptions (e.g. backend reviewer expecting SQLx idioms, test-writer-backend defaulting to SQLx integration tests, contract skill assuming a DB boundary, dep-audit assuming sqlx in Cargo.toml). For each, decide: gate behind detection, or add an "if your project uses a DB" caveat.
  3. **Dead surface area** — what ships to a no-DB project that's pure noise? Likely candidates: `just prepare-sqlx` recipe in `kit/common.just`, SQLx-specific reviewer rules, SQLx-flavored test-writer templates. Decide per item: ship-and-skip-when-irrelevant, gate at sync time, or split into a separate opt-in module.

  Output of this evaluation should be a concrete fix list (each item one of: graceful-skip, doc-gate, sync-time-exclude, or accept-as-noise) — not the fixes themselves. Related to issue #15 (graceful skip on empty stacks) but distinct: #15 is about _empty_ stacks during bootstrap; this is about _deliberately partial_ stacks (Tauri-without-DB) that are a permanent state, not a transient one.

- **SDD Phase 3 walk: align frontend agents/skills with v4.3 conventions.** Phases 1 (spec/plan/ADR) and 2 (backend) have already been walked — recent commits show `refactor(reviewer-backend)`, `refactor(reviewer-sql)`, `refactor(reviewer-arch)`, `refactor(test-writer-backend)`, `refactor(adr)` all "align with v4.3 conventions". Phase 3 (frontend) is next. Targets: `kit/agents/test-writer-frontend.md`, `kit/agents/reviewer-frontend.md`, `kit/skills/visual-proof/SKILL.md`, and optionally `kit/skills/setup-e2e/SKILL.md` (frontend-adjacent). For each: run the `ai-reviewer` agent on the file, apply the surfaced fixes (frontmatter discoverability, structural completeness, tool-grant minimality, density trimming, voice consistency), then `/preflight` to confirm cross-component coherence. Density signals from `check.py` already flag `test-writer-frontend.md` (379 lines, longest section 321 lines, 16 critical rules) as the heaviest in the kit — likely the most work.

## Experimental
